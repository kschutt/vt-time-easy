<p id="notice"><%= notice %></p>
<% if can? :manage, @program %>
<div class="pull-right">
	<div class="btn-group">
	<%= link_to "Edit", edit_program_path(@program), class: "btn btn-info" %>
	<%= link_to "New", new_program_path, class: "btn btn-success" %>
</div>
	</div>
	<% end %>
<h2><%= @program.name %></h2>

<%= render 'vtclasses/class_details' %>